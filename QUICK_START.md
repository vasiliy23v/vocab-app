# ⚡ БЫСТРЫЙ СТАРТ (5 минут)

## 🎯 Что Вам Нужно Сделать

### Шаг 1: Проверить Что Уже Есть (2 минуты)

**Откройте Supabase Dashboard:**
1. Перейдите в **SQL Editor**
2. **New query**
3. Скопируйте это:

```sql
SELECT 'Колод:' as type, COUNT(*) as count FROM public.decks
UNION ALL
SELECT 'Карточек:', COUNT(*) FROM public.cards
UNION ALL
SELECT 'Пользователей:', COUNT(*) FROM public.profiles;
```

4. Нажмите **Run** (Ctrl+Enter)
5. Посмотрите результат

**Что вы увидите:**
- Колод: X
- Карточек: Y
- Пользователей: Z

---

### Шаг 2: Применить Миграции (2 минуты)

**Скопируйте весь текст из файлов в порядке:**

#### 2.1 Миграция 0015
```
Файл: supabase/migrations/0015_multilingual_system.sql
```
- Откройте файл
- **Ctrl+A** (выделить всё)
- **Ctrl+C** (скопировать)
- В Supabase SQL Editor: **New query**
- **Ctrl+V** (вставить)
- **Ctrl+Enter** (запустить)
- ✅ Готово!

#### 2.2 Миграция 0016
```
Файл: supabase/migrations/0016_multilingual_import.sql
```
Повторите то же самое

#### 2.3 Миграция 0014 (если ещё не была применена)
```
Файл: supabase/migrations/0014_admin_manage_user_decks.sql
```
Повторите то же самое

---

### Шаг 3: Проверить Что Всё Работает (30 секунд)

В SQL Editor запустите:

```sql
-- Проверка 1
SELECT COUNT(*) as languages FROM public.languages;
-- Должно быть: 12

-- Проверка 2
SELECT COUNT(*) as functions FROM information_schema.routines
WHERE routine_name IN ('import_multilingual_cards', 'admin_add_language', 'admin_add_language_pair');
-- Должно быть: 3
```

**Если оба результата совпадают = ✅ ВСЁ ГОТОВО!**

---

### Шаг 4: Загрузить Пример (1 минута)

1. Откройте **Admin Panel** (в приложении)
2. Перейдите на вкладку **"Vocabulary"** (новая!)
3. Выберите **Student** из списка
4. Выберите или создайте **Deck**
5. Нажмите **"Add words"**
6. Загрузите файл: `examples_multilingual_ukrainian.csv`
7. ✅ Done!

---

## 📋 Что Если Не Получается?

### "Не вижу вкладку Vocabulary"
- Перезагрузите страницу (F5)
- Очистите кэш браузера (Ctrl+Shift+Delete)

### "SQL запрос выдаёт ошибку"
- Скопируйте ошибку и дайте мне
- Скорее всего миграция не применилась

### "Миграция не запускается"
- Проверьте что скопировали весь текст
- Попробуйте запустить ещё раз (команды идемпотентны)

---

## 🚀 Готово!

Теперь вы можете:
- ✅ Загружать слова на русском, английском, украинском
- ✅ Добавлять французский, португальский, любой язык
- ✅ Создавать пары (DE→UK, EN→RU, RU→UK и т.д.)
- ✅ Управлять всем из админ-панели

**Наслаждайтесь! 🎓**

---

## 📞 Помощь

Дайте мне:
1. Результат запроса из **Шага 1** (сколько у вас колод/карточек)
2. Скопированный текст ошибки (если есть)
3. Сказку какой шаг не получился

Я помогу! 💪
